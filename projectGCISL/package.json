{
  "name": "project-gcisl",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install && cd ../api && npm install",
    "build": "cd frontend && npm run build"
  }
}
